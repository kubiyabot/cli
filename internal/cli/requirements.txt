# Worker package with worker-specific dependencies only
# This installs the control plane API package with the [worker] extra
# which includes: temporalio, structlog, agno, litellm, psutil, docker, redis, mcp, nest-asyncio
kubiya-control-plane-api[worker]>=0.3.0

# Note: The kubiya-control-plane-api[worker] extra includes:
# - temporalio>=1.5.0 (Temporal workflow engine)
# - structlog>=24.1.0 (Structured logging)
# - agno>=2.0.10 (Agent orchestration)
# - litellm>=1.30.0 (LLM calls)
# - psutil>=5.9.0 (System monitoring)
# - docker>=7.0.0 (Docker tools support)
# - redis>=5.0.0 (Redis support)
# - mcp>=1.0.0 (MCP protocol)
# - nest-asyncio>=1.5.0 (Async support)
#
# Base dependencies from kubiya-control-plane-api:
# - httpx>=0.26.0 (HTTP client)
# - pydantic>=2.5.3 (Data validation)
